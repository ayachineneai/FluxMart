<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ayachinene.infra.product.persistence.sku.SkuMapper">

    <insert id="insertBatch">
        INSERT INTO sku
        (
            id,
            product_id,
            sku_code,
            merchant_sku_code,
            status,
            price_amount,
            image_file_id,
            version,
            created_at,
            updated_at
        )
        VALUES
        <foreach collection="list" item="sku" separator=",">
            (
                #{sku.id},
                #{sku.productId},
                #{sku.skuCode},
                #{sku.merchantSkuCode},
                #{sku.status},
                #{sku.priceAmount},
                #{sku.imageFileId},
                #{sku.version},
                #{sku.createdAt},
                #{sku.updatedAt}
            )
        </foreach>
    </insert>

    <select id="existsByProductCode" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM sku
            INNER JOIN product
                ON product.id = sku.product_id
            WHERE product.product_code = #{productCode}
            LIMIT 1
        )
    </select>

</mapper>
