<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ayachinene.infra.stock.persistence.StockReservationMapper">

    <select id="selectTargetsByOrderCode"
            resultType="org.ayachinene.infra.stock.persistence.StockReservationTarget">
        SELECT
            customer_order.id AS order_id,
            order_item.id AS order_item_id,
            sku.id AS sku_id,
            sku.sku_code
        FROM customer_order
        INNER JOIN order_item
            ON order_item.order_id = customer_order.id
        INNER JOIN sku
            ON sku.sku_code = order_item.sku_code
        WHERE customer_order.order_code = #{orderCode}
        ORDER BY order_item.sort_order
    </select>

    <insert id="insertBatch">
        INSERT INTO stock_reservation
        (
            id,
            order_id,
            order_item_id,
            sku_id,
            quantity,
            status,
            expires_at,
            created_at,
            updated_at
        )
        VALUES
        <foreach collection="list" item="reservation" separator=",">
            (
                #{reservation.id},
                #{reservation.orderId},
                #{reservation.orderItemId},
                #{reservation.skuId},
                #{reservation.quantity},
                #{reservation.status},
                #{reservation.expiresAt},
                #{reservation.createdAt},
                #{reservation.updatedAt}
            )
        </foreach>
    </insert>

</mapper>
