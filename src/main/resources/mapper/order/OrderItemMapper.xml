<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ayachinene.infra.order.persistence.OrderItemMapper">

    <insert id="insertBatch">
        INSERT INTO order_item
        (
            id,
            order_id,
            product_code,
            sku_code,
            product_title,
            specification_snapshot,
            image_file_id,
            unit_price_amount,
            quantity,
            total_amount,
            sort_order,
            created_at
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.id},
                #{item.orderId},
                #{item.productCode},
                #{item.skuCode},
                #{item.productTitle},
                #{item.specificationSnapshot},
                #{item.imageFileId},
                #{item.unitPriceAmount},
                #{item.quantity},
                #{item.totalAmount},
                #{item.sortOrder},
                #{item.createdAt}
            )
        </foreach>
    </insert>

</mapper>
